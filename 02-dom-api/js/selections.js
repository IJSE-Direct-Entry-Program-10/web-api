const outputElm = document.getElementById('output');
// outputElm.remove();

const liElmList = document.getElementsByTagName('li');

// for (let i = liElmList.length - 1 ; i >=0 ; i-- ){
//     liElm = liElmList[i];
//     liElm.remove();
// }

const elmList = document.getElementsByClassName('abc');

// for (let i = elmList.length - 1 ; i >=0 ; i-- ){
//     const liElm = elmList[i];
//     liElm.remove();
// }

const elmInput = document
    .querySelector('input[type="text"]');
// elmInput.remove();

const list = document.querySelectorAll('li');
// Array.from(list).forEach(elm => elm.remove());